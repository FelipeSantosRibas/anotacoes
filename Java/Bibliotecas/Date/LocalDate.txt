LocalDate date; // Instancia um objeto LocalDate

LocalDate.now(); // Retorna a data de agora
LocalDate.of(year,month,day); // Retorna a data especificada
LocalDate.parse("yyyy-MM-dd"); 

this.plusDays(); // Retorna a data mais x dias
this.plusWeeks(); // Retorna a data mais x semanas
this.plusMonths(); // Retorna a data mais x meses
this.plusYears(); // Retorna a data mais x anos

this.minusDays(); // Retorna a data menos x dias
this.minusWeeks();
this.minusMonths();
this.minusYears();

this.isBefore(); // Retorna true se this for antes da data especificada
this.isAfter(); // Retorna true se this for depois da data especificada
this.isEqual(); // Retorna true se this for a mesma data que a especificada

this.format(DateTimeFormatter); // Formata de acordo com um DateTimeFormatter