@JsonAlias("") // Nome no processo de deserialização (Pode colocar um array de Strings)
@JsonProperty("") // Nome no processo de serialização e deserialização

@JsonIgnoreProperties(ignoreUnknown = true) // Ignorar propriedades não especificadas

--------------------------------------------------------------

JsonNode:

ObjectMapper mapper = new ObjectMapper();
JsonNode node = mapper.readTree(response.body());

traducao = node
        .get("data")
        .get("translations")
        .get(0)
        .get("translatedText")
        .asText();

ou

traducao = node.at("/data/translations/0/translatedText").asText();